import React from "react";
import Base from "../core/Base";

const userDashBoard = () => {
  return (
    <Base Title="UserDashBoard" description="This is dashboard for each user">
      <h1>This is User userDashBoard</h1>
    </Base>
  );
};

export default userDashBoard;